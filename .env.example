# =============================================================
# i-factory — Environment Variables
#
# Each service has its own .env file. Copy the .env.example
# from each service directory and fill in your values:
#
#   apps/api/.env.example  →  apps/api/.env
#   apps/web/.env.example  →  apps/web/.env
# =============================================================
