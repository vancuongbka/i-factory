{
  "name": "@i-factory/utils",
  "version": "0.0.1",
  "private": true,
  "description": "Pure TypeScript utilities — zero framework dependencies",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "jest",
    "lint": "eslint src/"
  },
  "dependencies": {
    "@i-factory/api-types": "workspace:*"
  },
  "devDependencies": {
    "@i-factory/config": "workspace:*",
    "typescript": "^5.5.0"
  }
}
